global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'sentry-agent'
    static_configs:
      - targets: ['172.30.0.30:8080']
        labels:
          component: 'ai-agent'
